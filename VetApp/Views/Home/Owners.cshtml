@model List<Owner>
@{
    ViewData["Title"] = "Owners";
    ViewData["OwnersActive"] = "active";
}

<h2>Owners</h2>
@if (Model.Count == 0)
{
    <h4>Without owners in the Database.</h4>
    <a asp-controller="NewEntries" asp-action="AddOwner">Add a new one.</a>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">LicenseNumber</th>
                <th scope="col">Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var owner in Model)
            {
                <tr>
                    <th scope="row">@owner.Id</th>
                    <td><a asp-action="OwnerDetails" asp-route-id="@owner.Id">@owner.Name</a></td>
                </tr>
            }
        </tbody>
    </table>

}





